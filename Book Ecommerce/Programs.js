function particularCharacter(string,char) {
   
    let answer

   string.forEach((character) => {
        
    if(char == character)

        answer = character
    })

    return answer
    
}